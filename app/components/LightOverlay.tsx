// White wash that sits above the WebGL nebula, below the app glass, in light
// mode (styled by #vm-light-overlay in globals.css).
export function LightOverlay() {
  return <div id="vm-light-overlay" aria-hidden="true" />;
}
